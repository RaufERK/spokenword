[Unit]
Description=Audio-only HLS for %i
After=network.target nginx.service

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/start-audio-hls.sh %i
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target


