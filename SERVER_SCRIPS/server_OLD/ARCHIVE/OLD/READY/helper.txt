🎉 Отлично, теперь всё крутится, потому что сошлись три ключевые вещи:

Что было «не так» раньше	Что мы поправили	Эффект
nginx‑rtmp не стартовал скрипт → в rtmp://127.0.0.1/ / терялись $app/$name, а в логах не было exec:	Вернули директиву exec /usr/local/bin/start-hls.sh $app $name; и перезагрузили nginx	nginx снова вызывает скрипт при каждом publish
Старый start-hls.sh содержал пустую строку ' ' после \ → FFmpeg получал битый список опций	Переписали скрипт без лишних \ и с полным путём к FFmpeg 7	FFmpeg успешно запускается и пишет сегменты
В каталоге лежал пустой «залипший» playlist.m3u8 ⇒ плеер видел 0 байт	Очистили /srv/streaming/live и задали
-hls_list_size 6 + delete_segments	Каждый новый эфир формирует свежий плейлист, старые сегменты удаляются

Итоговая цепочка теперь работает так
OBS подключается → nginx‑rtmp пишет client publish

nginx выполняет exec start-hls.sh live ITMUG2025

FFmpeg 7 берёт поток, каждые 4 с создаёт seg00000.ts… и
формирует плейлист на 6 последних сегментов

Nginx отдаёт /live/playlist.m3u8, Hls.js видит изменения и играет
актуальный эфир

Когда стрим останавливается, скрипт завершаетcя, старые .ts
автоматически удаляются → плеер корректно показывает «offline»

Поэтому сейчас работает: директива exec снова активна, скрипт
исправен и находит FFmpeg, плейлист формируется без ошибок и отдается
плееру. Сохрани текущие конфиги (из «победного списка») — это базовая,
надежная версия, от которой можно спокойно развивать дальше. Удачных
стримов!
