# Кандидаты на удаление — Чистка проекта

Дата анализа: 2026-04-11

Цель: сократить расход токенов при работе с AI-моделями, удалив неиспользуемые файлы.

---

## 1. AI-документы / заметки (не код)

| Файл | Статус | Решение |
|------|--------|---------|
| `mirror_gate_recommendations.txt` | Заметки AI про Mirror Gate план | ❓ |
| `upload-service/mirror_gate_recommendations.txt` | Дубликат того же файла | ❓ |
| `PLANS/ACTIVE_INFRASTRUCTURE_PLAN.md` | Плановый документ инфраструктуры | ❓ |
| `PLANS/POSTGRESQL_MIGRATION_PLAN.md` | План миграции на PostgreSQL | ❓ |

---

## 2. Build-артефакты

| Файл | Статус | Решение |
|------|--------|---------|
| `tsconfig.tsbuildinfo` | Генерируется автоматически при сборке | ✅ Удалить |

---

## 3. Тестовые файлы (медиа)

| Файл | Статус | Решение |
|------|--------|---------|
| `public/test-uploads/test_20251229130232_a0417e.mp4` | Тестовая загрузка из декабря 2025 | ✅ Удалить |
| `public/test-uploads/test_20251229130239_a99f53.mp4` | Тестовая загрузка из декабря 2025 | ✅ Удалить |

---

## 4. Неиспользуемые компоненты (нигде не импортируются)

| Файл | Статус | Решение |
|------|--------|---------|
| `components/IcecastPlayer.tsx` | Не импортируется ни в одном файле | ✅ Удалить |
| `components/StreamLinkBlock.tsx` | Не импортируется ни в одном файле | ✅ Удалить |
| `components/ArchiveList.tsx` | Не импортируется (рабочий — в `app/conf-arch/ArchiveList.tsx`) | ✅ Удалить |
| `components/ArchiveConfList.tsx` | Не импортируется ни в одном файле | ✅ Удалить |
| `components/Announcement.tsx` | Закомментирован в `conf/page.tsx`, не импортируется активно | ✅ Удалить |
| `components/user/AudioPlayer.tsx` | Не импортируется (используется только `VideoPlayer.tsx`) | ✅ Удалить |

---

## 5. Мёртвый файл без точки применения

| Файл | Статус | Решение |
|------|--------|---------|
| `proxy.ts` | Экспортирует `proxy()` но **нет `middleware.ts`** который бы импортировал его — защита маршрутов не работает. Нужно либо переименовать в `middleware.ts`, либо удалить. | ⚠️ Решить отдельно |

---

## 6. Директория-прототип FigmaV6 (~80 файлов)

| Директория | Статус | Решение |
|-----------|--------|---------|
| `FigmaV6/` | Отдельный Vite/React прототип дизайна. Никак не связан с основным Next.js приложением. Содержит полный набор shadcn/ui компонентов, маршруты, стили — всё изолировано. | ❓ |

---

## Итог по статусам

| Статус | Файлов |
|--------|--------|
| ✅ Можно удалить без сомнений | ~10 файлов |
| ❓ Требует решения | ~84 файла (FigmaV6 + PLANS + mirror_gate docs) |
| ⚠️ Требует отдельного действия | 1 файл (`proxy.ts`) |
